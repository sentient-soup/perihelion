# Placeholder. Regenerate with:  ./k8s/scripts/generate-secret.sh apseline
# (reads APSELINE_* + CLOUDFLARE_API_TOKEN from k8s/.env, seals offline).
# Keys: SESSION_SECRET, CLOUDFLARE_API_TOKEN, OAUTH_CLIENT_SECRET (empty if OAuth off).
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: apseline-secrets
  namespace: apps
spec:
  encryptedData:
    SESSION_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
    CLOUDFLARE_API_TOKEN: REPLACE_WITH_KUBESEAL_OUTPUT
    OAUTH_CLIENT_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: apseline-secrets
      namespace: apps
