# Sink secrets. Copy to .secrets.env and fill in (workflow: README.md).
# .secrets.env is gitignored — never commit real values.

# Gates the entire API + uploads. Recommended since sink is exposed via Traefik.
# Any long random string. Generate one: openssl rand -hex 32
SINK_TOKEN=REPLACE_ME
