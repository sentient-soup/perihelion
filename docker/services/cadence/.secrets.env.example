# Cadence secrets. Copy to .secrets.env and fill in (workflow: README.md).
# .secrets.env is gitignored — never commit real values.

# Signs the auth JWTs. Any long random string; changing it invalidates all
# existing logins. Generate one: openssl rand -hex 32
JWT_SECRET=REPLACE_ME
