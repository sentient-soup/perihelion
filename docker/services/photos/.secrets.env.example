# Photos (Immich) secrets. Copy to .secrets.env and fill in (workflow: README.md).
# .secrets.env is gitignored — never commit real values.

# Immich Postgres database password
IMMICH_DB_PASSWORD=REPLACE_ME
