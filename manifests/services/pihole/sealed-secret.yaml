# Generate this file with kubeseal. Do not commit plain secrets.
#
# 1. Create the plain secret (do NOT commit this file):
#    kubectl create secret generic pihole-secrets \
#      --namespace networking \
#      --from-literal=PIHOLE_PASSWORD=<your-password> \
#      --dry-run=client -o yaml > /tmp/pihole-secrets.yaml
#
# 2. Seal it:
#    kubeseal --cert pub-cert.pem \
#      --namespace networking \
#      < /tmp/pihole-secrets.yaml > manifests/services/pihole/sealed-secret.yaml
#
# 3. Commit this file. Delete /tmp/pihole-secrets.yaml.
#
# Replace the contents below with the output of kubeseal:
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: pihole-secrets
  namespace: networking
spec:
  encryptedData:
    PIHOLE_PASSWORD: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: pihole-secrets
      namespace: networking
