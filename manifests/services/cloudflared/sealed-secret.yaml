# Generate this file with kubeseal. Do not commit plain secrets.
#
# 1. Create the plain secret (do NOT commit this file):
#    kubectl create secret generic cloudflared-secrets \
#      --namespace networking \
#      --from-literal=TUNNEL_TOKEN=<your-token> \
#      --dry-run=client -o yaml > /tmp/cloudflared-secrets.yaml
#
# 2. Seal it:
#    kubeseal --cert pub-cert.pem \
#      --namespace networking \
#      < /tmp/cloudflared-secrets.yaml > manifests/services/cloudflared/sealed-secret.yaml
#
# Replace the contents below with the output of kubeseal:
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cloudflared-secrets
  namespace: networking
spec:
  encryptedData:
    TUNNEL_TOKEN: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: cloudflared-secrets
      namespace: networking
